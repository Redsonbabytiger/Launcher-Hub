name: Build, Release and Publish APT Repo

on:
  push:
    tags:
      - "v*.*"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up build environment
        run: |
          sudo apt-get update
          sudo apt-get install -y dpkg-dev fakeroot make

      - name: Build .deb package
        run: |
          make clean build
          mkdir -p repo/pool/main/l
          mv *.deb repo/pool/main/l/

      - name: Generate APT repo metadata
        run: |
          mkdir -p repo/dists/stable/main/binary-amd64
          dpkg-scanpackages repo/pool /dev/null > repo/dists/stable/main/binary-amd64/Packages
          gzip -9c repo/dists/stable/main/binary-amd64/Packages > repo/dists/stable/main/binary-amd64/Packages.gz

      - name: Upload APT repo to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./repo
          publish_branch: gh-pages

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: repo/pool/main/l/*.deb
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
